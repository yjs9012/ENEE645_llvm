Team memeber : Jinsub Yim, Song yuhao

This is function instantiate pass for the project

1. Instantiate3.cpp : source code for pass
2. Makefile(in inst3) : makefile in lib/Instante3 directory
3. Makefile(in lib) : makefile in lib directory
4. samples.tar.gz : having samples before and after opimization
                    ex) before : example_${suffix}.bc / after : ex_${suffix}Ist.bc
                    for better understanding we include human readable file too.
